# nixpacks.toml — tells Railway how to build and start this app

[phases.install]
cmds = [
  "npm install --prefix backend",
  "npm install --prefix frontend",
  "npm run build --prefix frontend"
]

[variables]
NODE_ENV = "production"

[start]
cmd = "node backend/server.js"
